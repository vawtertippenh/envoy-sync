// Package envtag provides functionality to label and categorise env keys
// using tag rules based on exact matches or prefix patterns.
//
// Tags can be used to group keys by concern (e.g. "database", "app", "logging")
// and produce human-readable summaries of how an env file is structured.
package envtag
